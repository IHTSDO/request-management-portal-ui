import { RequestTypeTransformPipe } from './request-type-transform.pipe';

describe('RequestTypeTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new RequestTypeTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
